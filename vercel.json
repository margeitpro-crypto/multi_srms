{
  "version": 2,
  "builds": [
    {
      "src": "backend/vercel-server.ts",
      "use": "@vercel/node",
      "config": {
        "includeFiles": [
          "dist/**",
          "backend/**",
          "services/**",
          "routes/**",
          "middleware/**"
        ]
      }
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/backend/vercel-server.ts"
    },
    {
      "src": "/(.*)",
      "dest": "/backend/vercel-server.ts"
    }
  ]
}